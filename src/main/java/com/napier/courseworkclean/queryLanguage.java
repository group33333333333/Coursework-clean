package com.napier.courseworkclean;

import java.sql.*;
import java.util.ArrayList;

public class queryLanguage {


    // Methods to solve final SQL query
    // Requires JOIN Country and Language
    // something like the following?
    // SUM (%population WHERE language = "CHINESE") * (totalPop WHERE language.countrycode = country.code)
    // GROUP BY country name
    // ORDER BY totalSpeakers ASC


}



